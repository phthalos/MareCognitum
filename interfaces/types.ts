export type Children = {
    children?: React.ReactNode;
};
export type Content = {
    content: string;
};
export type AlertType = {
    buttonName: string;
    dialogTitle: string;
    content: string;
    confirmBtnName: string;
    className: string;
};
